<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="container">
    <h1><%= message %></h1>
    
    <div class="quote-box">
      <p class="quote-text"><%= quote ? quote.quote : '' %></p>
      <p class="quote-author"><%= quote ? `— ${quote.author}` : '' %></p>
    </div>
    
    <img class="dog-image" src="<%= dogImage ? dogImage.url : '' %>" alt="Random dog">

    <button type="button" id="refresh-btn">Get New Content</button>
  </div>

  <script src="app.js"></script>
</body>
</html>